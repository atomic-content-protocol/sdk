/**
 * ACP Core — public type exports.
 *
 * Re-exports all runtime interfaces from the types sub-module so that
 * consumers can import from `@acp/core` or `@acp/core/types` without
 * needing to know the internal file layout.
 */
export type { ACO, Container, Collection, ACOParseResult } from "./aco.js";
